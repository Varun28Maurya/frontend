import 'package:flutter/material.dart';

class OwnerDocsPage extends StatelessWidget {
  const OwnerDocsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Owner Docs"),
    );
  }
}
