import 'package:flutter/material.dart';

class OwnerProjectsPage extends StatelessWidget {
  const OwnerProjectsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Owner Projects"),
    );
  }
}
