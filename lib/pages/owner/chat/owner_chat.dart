import 'package:flutter/material.dart';

class OwnerChatPage extends StatelessWidget {
  const OwnerChatPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Owner Chat"),
    );
  }
}
